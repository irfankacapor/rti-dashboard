package io.dashboard.enums;

public enum ImpactDirection {
    POSITIVE,  // Higher indicator values = better goal progress
    NEGATIVE   // Lower indicator values = better goal progress (e.g., unemployment rate)
} 